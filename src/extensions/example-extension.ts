
import { GluegunToolbox } from 'gluegun'

module.exports = (toolbox: GluegunToolbox) => {
  // enable this if you want to read configuration in from
  // the current folder's package.json (in a "flutter-cli" property),
  // flutter-cli.config.json, etc.
  // toolbox.config = {
  //   ...toolbox.config,
  //   ...toolbox.config.loadConfig(process.cwd(), "flutter-cli")
  // }
};
